cron:
- description: hourly feed update
  url: /services/crawl_feeds
  schedule: every 10 minutes synchronized
  
- description: check to send emails
  url: /services/check_sendmail
  schedule: every 1 minutes synchronized
